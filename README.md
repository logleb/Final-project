## Setup

1. Install deps: `pip install -r requirements.txt`
2. Run the app: `python app.py`
3. Open the app at `http://127.0.0.1:5000`

The SQLite file already includes sample admins (`admin1`/`12345`, etc.) and some reservations.
